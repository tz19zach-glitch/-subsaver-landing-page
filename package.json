{
  "name": "subsaver-landing-phase0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check api/waitlist.js && node --check api/event.js && node --check api/_lib/http.js && node --check api/_lib/supabase.js"
  }
}
